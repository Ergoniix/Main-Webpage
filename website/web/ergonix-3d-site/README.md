# Ergonix 3D Website

A polished Next.js + React Three Fiber landing page rebuilt from the supplied source fragments.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Production

```bash
npm run build
npm start
```

The `/api/health` endpoint returns a simple JSON health response and does not require a database.
